---
export interface Props {
  title?: string;
  subtitle?: string;
  headline?: string;
  description?: string;
  primaryCTA?: {
    text: string;
    href: string;
  };
  secondaryCTA?: {
    text: string;
    href: string;
    phone?: boolean;
  };
  variant?: string;
}

const {
  title,
  subtitle,
  headline,
  description,
  primaryCTA,
  secondaryCTA,
  variant = 'default'
} = Astro.props;

// Use headline/description if provided, otherwise fall back to title/subtitle
const displayTitle = headline || title;
const displaySubtitle = description || subtitle;
---

<section id="contact" class="py-20 gradient-seamless text-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-4xl md:text-5xl font-bold mb-4">{displayTitle}</h2>
    {displaySubtitle && (
      <p class="text-xl text-white mb-8">
        {displaySubtitle}
      </p>
    )}
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      {primaryCTA && (
        <a href={primaryCTA.href} class="btn-primary inline-flex items-center justify-center">
          <span style="color: #0C3745 !important;">{primaryCTA.text}</span>
          <i data-lucide="arrow-right" class="w-5 h-5 ml-2" style="color: #0C3745 !important;"></i>
        </a>
      )}
      {secondaryCTA && (
        <a href={secondaryCTA.href} class="btn-secondary inline-flex items-center justify-center">
          {secondaryCTA.phone && <i data-lucide="phone" class="w-5 h-5 mr-2" style="color: #0C3745 !important;"></i>}
          <span style="color: #0C3745 !important;">{secondaryCTA.text}</span>
        </a>
      )}
    </div>
  </div>
</section>